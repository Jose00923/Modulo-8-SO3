date

clear

sudo dnf -y install dnf-plugins-core (nos proporciona un paquete de repositorios para docker)

sudo dnf-3 config-manager --add-repo https://download.docker.com/linux/fedora/docker-ce.repo

sudo dnf makecache (para actualizar los repositorios)

sudo dnf install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

docker version

systemctl status docker

systemctl start docker

systemctl enable --now docker

docker ps -a

entramos a ducker hub

docker images

sudo docker run -d -p 8888:80 nginx

docker ps

127.0.0.1:8888

sudo docker rm

cd /home/josejerez/website/

sudo docker run -d -p 8888:80 -v /home/josejerez/website:/usr/share/nginx/html --name cocacola nginx

docker ps

ls

nano index.html

<html>
<body>
<h1>Jose Jerez</h1>
<h3>20250859</h3>
</body> 
</html>

clear

date